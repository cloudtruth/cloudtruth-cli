```console
$ cloudtruth versions --help
cloudtruth-versions 
Manage CloudTruth CLI versions

USAGE:
    cloudtruth versions [SUBCOMMAND]

FLAGS:
    -h, --help       Prints help information
    -V, --version    Prints version information

SUBCOMMANDS:
    check      Check the CLI is running the latest version [aliases: ch, c]
    get        Get the running CLI version
    help       Prints this message or the help of the given subcommand(s)
    install    Update to the latest CLI version [aliases: inst, in, i]

```