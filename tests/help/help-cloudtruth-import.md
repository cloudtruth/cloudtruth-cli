```console
$ cloudtruth import --help
cloudtruth-import 
Perform imports into the CloudTruth environment

USAGE:
    cloudtruth import [SUBCOMMAND]

FLAGS:
    -h, --help       Prints help information
    -V, --version    Prints version information

SUBCOMMANDS:
    help          Prints this message or the help of the given subcommand(s)
    parameters    Import parameter values into a specified project and environment [aliases: parameter, param, par,
                  pa, p]

```