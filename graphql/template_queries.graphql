query GetTemplateByNameQuery($environmentName: String, $templateName: String!) {
  viewer {
    template(name: $templateName) {
      evaluated(environmentName: $environmentName)
    }
  }
}


query TemplatesQuery {
  viewer {
    id
    templates {
      nodes {
        id
        name
      }
    }
  }
}
