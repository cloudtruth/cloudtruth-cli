query IntegrationsQuery($organizationId: ID) {
  viewer {
    id
    organization(id: $organizationId) {
      integrations {
        nodes {
          __typename
          id
          fqn
          name
        }
      }
    }
  }
}
